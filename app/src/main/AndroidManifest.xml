<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="woo.sopt22.kotlinexam">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SeekBar.SeekBarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Recyclerview2.Main3Activity" />
        <activity android:name=".ViewPager.Main4Activity" />
        <activity android:name=".Progress.Main6Activity" />
        <activity android:name=".Spinner.Main5Activity" />
        <activity android:name=".Scrollview.Main7Activity"></activity>
    </application>

</manifest>