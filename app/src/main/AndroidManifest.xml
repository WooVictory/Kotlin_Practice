<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="woo.sopt22.kotlinexam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> <!--셀룰러나 와이파이로 위치 측정 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> <!--GPS로 위치 측정-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Recyclerview2.RecyclerViewActivity" />
        <activity android:name=".ViewPager.ViewPagerActivity" />
        <activity android:name=".Progress.ProgressActivity" />
        <activity android:name=".Spinner.SpinnerActivity" />
        <activity android:name=".Scrollview.ScrollActivity" />
        <activity android:name=".SeekBar.SeekBarActivity" />
        <activity android:name=".VideoView.VideoActivity" />
        <activity android:name=".Dialog.DialogActivity" />
        <activity android:name=".SurfaceView.SurfaceViewActivity" />
        <activity android:name=".Picker.DateTimePickerActivity" />
        <activity android:name=".WebView.WebViewActivity" />
        <activity android:name=".GoogleMap.GoogleMapActivity"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDk_eINabNebdfWaQpUMuUWmBdNLyuAayc"/>
    </application>

</manifest>